# 小红书短链跳转服务 - Vercel 部署准备完成

## 目标
将小红书短链跳转服务部署到 Vercel 线上服务器。

## 已完成工作

### 1. 项目文件准备
- `server.js` - Express 服务端（已适配 Vercel 无服务器架构）
- `vercel.json` - Vercel 配置文件
- `package.json` - 项目依赖
- `.gitignore` - 忽略 node_modules
- `DEPLOY_GUIDE.md` - 详细部署指南

### 2. Vercel 适配
- 导出 `app` 对象供 Vercel 使用
- 本地开发时保持原有启动方式
- 配置 `vercel.json` 路由规则

### 3. Git 仓库初始化
- 已初始化 Git 仓库
- 已提交所有文件
- 已排除 node_modules

## 部署步骤

### 用户需要做的：

1. **创建 GitHub 仓库**
   - 访问 https://github.com/new
   - 创建仓库 `xhs-redirect`

2. **推送代码到 GitHub**
   ```bash
   cd /Users/chenpeng/.qclaw/workspace/xhs-redirect
   git remote add origin https://github.com/你的用户名/xhs-redirect.git
   git push -u origin master
   ```

3. **在 Vercel 部署**
   - 访问 https://vercel.com/new
   - 导入 GitHub 仓库
   - 点击 Deploy

4. **获取域名**
   - 部署完成后获得 `xxx.vercel.app` 域名

## 使用示例

部署后访问：
```
https://你的域名.vercel.app/redirect?param=https://xhslink.com/m/AZybE6hgByh
```

## 文件位置

`/Users/chenpeng/.qclaw/workspace/xhs-redirect/`

## 下一步

用户需要：
1. 创建 GitHub 账号（如果没有）
2. 创建仓库并推送代码
3. 在 Vercel 部署

或者使用 Vercel CLI 直接部署（需要安装 CLI）。
